# PRT vs PBR: 为什么渲染效果不同？

## 核心问题
你的疑惑是合理的：**PRT 不仅仅是 PBR 的预计算优化版本，它们是完全不同的着色模型**。

---

## 1. PBR (Physically Based Rendering) - 物理基础渲染

### 工作原理
```
PBR 计算流程：
┌─────────────────────────────────────────────────────┐
│ 对于每个像素，对于每个光源：                          │
├─────────────────────────────────────────────────────┤
│ 1. 获取光源方向 L（实时计算）                        │
│ 2. 获取视线方向 V（实时计算）                        │
│ 3. 计算表面法线 N                                    │
│ 4. 根据 L, V, N 计算 BRDF（Cook-Torrance）          │
│ 5. 计算光照贡献：L_out = BRDF × L_in × (N·L)        │
│ 6. 累加所有光源的贡献                                │
└─────────────────────────────────────────────────────┘
```

### 关键特性
- **实时响应光源位置变化**：当光源移动时，BRDF 会改变，着色效果立即更新
- **完整的物理模型**：包含 Fresnel、Roughness、Metallic 等参数
- **计算成本高**：每帧都要计算 BRDF

### 数学表达
```glsl
// PBR 核心计算（简化版）
vec3 L = normalize(lightPos - fragPos);  // 实时计算
vec3 V = normalize(viewPos - fragPos);   // 实时计算
vec3 H = normalize(L + V);

float NdotL = max(dot(N, L), 0.0);
float NdotV = max(dot(N, V), 0.0);
float NdotH = max(dot(N, H), 0.0);

// BRDF 依赖于 L 和 V 的方向
vec3 brdf = calculateBRDF(NdotL, NdotV, NdotH, roughness, metallic);
vec3 color = brdf * lightColor * NdotL;
```

---

## 2. PRT (Precomputed Radiance Transfer) - 预计算辐射转移

### 工作原理
```
PRT 计算流程：

【离线预计算阶段】
┌──────────────────────────────────────────────────┐
│ 1. 将光照环境分解为球谐函数（SH）系数            │
│ 2. 对于每个顶点，预计算其对每个 SH 系数的响应    │
│ 3. 存储这些系数（PRT 系数）                      │
│ 4. 保存到文件或内存                              │
└──────────────────────────────────────────────────┘
                    ↓
【运行时渲染阶段】
┌──────────────────────────────────────────────────┐
│ 1. 获取当前光照环境的 SH 系数                    │
│ 2. 与预计算的 PRT 系数做点积                     │
│ 3. 直接得到着色结果                              │
│ 计算：color = dot(SH_coeffs, PRT_coeffs)        │
└──────────────────────────────────────────────────┘
```

### 关键特性
- **预计算的光照响应**：光照信息被编码到 SH 系数中
- **限制性光源**：只能处理预计算时设定的光照环境
- **计算成本低**：运行时只需简单的点积操作
- **无法实时响应动态光源**：光源位置改变时，着色效果不会改变

### 数学表达
```glsl
// PRT 核心计算
// 离线预计算：
// PRT_coeff[i] = ∫∫ BRDF(L,V) × visibility(L) × SH_basis_i(L) dω

// 运行时：
vec3 color = vec3(0.0);
for(int i = 0; i < SH_BANDS; i++) {
    color += PRT_coeffs[i] * SH_coeffs_current[i];
}
```

---

## 3. 为什么渲染效果不同？

### 关键区别对比表

| 特性 | PBR | PRT |
|------|-----|-----|
| **光源响应** | ✅ 实时响应光源位置变化 | ❌ 无法响应动态光源 |
| **光照信息** | 实时计算 | 预计算编码到 SH 系数 |
| **BRDF 计算** | 每帧计算 | 预计算到系数中 |
| **视线依赖** | ✅ 考虑视线方向 V | ❌ 通常不考虑视线方向 |
| **计算成本** | 高 | 低 |
| **灵活性** | 高 | 低 |
| **动态光源** | ✅ 支持 | ❌ 不支持 |

### 你的两张图片对比

**图1（Light Rotation = 1.178）**
- 白色光源在右上方
- PBR 会根据光源位置计算每个像素的着色
- 物体右侧面向光源，会更亮
- 物体左侧背离光源，会更暗

**图2（Light Rotation = 0.000）**
- 白色光源在下方
- PBR 重新计算所有像素的 BRDF
- 物体下侧面向光源，会更亮
- 物体上侧背离光源，会更暗

**如果是 PRT：**
- 两张图片的着色效果会**完全相同**
- 因为 PRT 系数是预计算的，不会因为光源移动而改变
- 只有当改变预计算的光照环境时，才会看到不同的效果

---

## 4. 为什么你的实现中 PRT 显示"Inactive"？

### 原因分析
```
PRT Status: Inactive
↓
这意味着：
1. PRT 预计算还未完成
2. 或者 PRT 系数还未加载
3. 系统仍在使用 PBR 进行渲染
```

### 要使 PRT 工作，需要：
1. **预计算阶段**：离线计算 PRT 系数
2. **存储**：将系数保存到文件
3. **加载**：运行时加载这些系数
4. **使用**：在着色器中使用预计算系数

---

## 5. 为什么 PRT 的效果"不如" PBR？

### 这不是 BUG，而是 PRT 的设计限制

```
PRT 的权衡：
┌─────────────────────────────────────────────────┐
│ 优势：                                          │
│ • 渲染速度快（只需点积）                        │
│ • 支持复杂的全局光照（GI）                      │
│ • 支持动态阴影                                  │
├─────────────────────────────────────────────────┤
│ 劣势：                                          │
│ • 无法实时响应光源位置变化                      │
│ • 无法处理完全动态的光源                        │
│ • 预计算成本高                                  │
│ • 存储成本高（需要存储 SH 系数）                │
└─────────────────────────────────────────────────┘
```

---

## 6. 实际应用场景

### PBR 适合：
- ✅ 动态光源场景（如手电筒、汽车灯）
- ✅ 实时光源移动
- ✅ 光源数量变化
- ✅ 需要精确物理模型

### PRT 适合：
- ✅ 静态或缓慢变化的光照环境
- ✅ 需要全局光照（GI）的场景
- ✅ 需要高性能的移动设备
- ✅ 预计算成本可以接受

---

## 7. 总结

**PRT 不是 PBR 的优化版本，而是完全不同的方法：**

```
PBR:  L(实时) + V(实时) + BRDF(实时) = 动态、灵活、计算量大
PRT:  预计算系数 + 点积(实时) = 静态、高效、灵活性低
```

你看到的效果差异是**正常的**，因为：
1. **PBR** 能实时响应光源位置变化
2. **PRT** 的着色结果是预计算的，不会因为光源移动而改变

这不是实现错误，而是两种技术的根本差异。

---

## 8. 如何验证你的理解

### 测试 PBR（应该看到效果变化）
1. 启用 PBR 模式
2. 移动光源（改变 Light Rotation）
3. **观察**：物体的亮暗分布会改变 ✅

### 测试 PRT（应该看到效果不变）
1. 启用 PRT 模式（如果已预计算）
2. 移动光源（改变 Light Rotation）
3. **观察**：物体的着色效果保持不变 ✅

如果 PRT 也会随光源改变，那说明 PRT 还未正确启用。

